import { useState } from "react";

interface ShowOnMapsProps {
  icao24: string;
  latitude: number;
  longitude: number;
  speed: number;
  callsign: string;
}
function showOnMap({ icao24, latitude, longitude, speed, callsign }: ShowOnMapsProps) {

  return(

  )
}
